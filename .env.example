NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional. Defaults to http://localhost:3000 in the browser.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# TODO: Add these values in Vercel Project Settings before deploying.
# TODO: Configure Google OAuth in Supabase Auth and add the deployed Vercel URL as an allowed redirect URL.
